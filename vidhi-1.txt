<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>For Vidhi ❤️</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
body {
  margin: 0;
  font-family: 'Segoe UI', sans-serif;
  background: linear-gradient(to bottom, #1a1a2e, #16213e, #0f3460);
  color: white;
  overflow-x: hidden;
  scroll-behavior: smooth;
}

section {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 40px;
  opacity: 0;
  transform: translateY(40px);
  transition: all 1.3s ease;
}

section.show {
  opacity: 1;
  transform: translateY(0);
}

h1 {
  font-size: 2.6em;
  margin-bottom: 20px;
}

p {
  font-size: 1.2em;
  max-width: 750px;
  line-height: 1.8;
}

/* Glowing photos */
img {
  width: 280px;
  height: 280px;
  object-fit: cover;
  border-radius: 25px;
  margin: 25px 0;
  box-shadow:
    0 0 20px rgba(255,182,193,0.4),
    0 0 40px rgba(255,105,180,0.25);
  animation: glowPulse 4s ease-in-out infinite;
}

@keyframes glowPulse {
  0% {
    box-shadow:
      0 0 15px rgba(255,182,193,0.3),
      0 0 30px rgba(255,105,180,0.2);
  }
  50% {
    box-shadow:
      0 0 30px rgba(255,182,193,0.6),
      0 0 60px rgba(255,105,180,0.4);
  }
  100% {
    box-shadow:
      0 0 15px rgba(255,182,193,0.3),
      0 0 30px rgba(255,105,180,0.2);
  }
}

/* Floating hearts */
.heart {
  position: fixed;
  bottom: -20px;
  font-size: 20px;
  animation: floatUp 14s linear infinite;
  opacity: 0.7;
}

@keyframes floatUp {
  0% { transform: translateY(0); opacity: 0; }
  10% { opacity: 1; }
  100% { transform: translateY(-120vh); opacity: 0; }
}

/* Music button */
#musicBtn {
  position: fixed;
  top: 15px;
  right: 15px;
  background: rgba(255,255,255,0.15);
  border: none;
  color: white;
  padding: 10px 15px;
  border-radius: 30px;
  cursor: pointer;
  font-size: 14px;
  backdrop-filter: blur(5px);
  z-index: 10;
}

/* Candle letter section */
.letter {
  background: radial-gradient(circle at top, #2b1b0f, #0b0603);
  box-shadow: inset 0 0 120px rgba(255,140,0,0.15);
  position: relative;
  overflow: hidden;
}

.candle {
  position: absolute;
  bottom: 0;
  width: 6px;
  height: 40px;
  background: #ffd7a0;
  border-radius: 3px;
  opacity: 0.7;
}

.candle::before {
  content: "";
  position: absolute;
  top: -14px;
  left: -7px;
  width: 20px;
  height: 20px;
  background: rgba(255,165,0,0.8);
  border-radius: 50%;
  filter: blur(6px);
  animation: flicker 1.8s infinite alternate;
}

@keyframes flicker {
  from { transform: scale(1); opacity: 0.7; }
  to { transform: scale(1.3); opacity: 1; }
}
</style>
</head>

<body>

<button id="musicBtn">🔊 Music</button>

<audio id="bgMusic" loop autoplay muted>
  <source src="music.mp3" type="audio/mpeg">
</audio>

<!-- INTRO -->
<section>
  <h1 id="typeText"></h1>
  <p>
    This page exists for one reason — you.<br>
    Every scroll is a memory.<br>
    Every word is how I feel about you.
  </p>
</section>

<!-- DATE -->
<section>
  <h1>11 • 11 • 2024</h1>
  <p>
    The day you told me how you felt.<br>
    My world changed forever ❤️
  </p>
</section>

<!-- PHOTOS -->
<section><img src="photo1.jpg"><p>You became my favorite feeling.</p></section>
<section><img src="photo2.jpg"><p>Even silence feels beautiful with you.</p></section>
<section><img src="photo3.jpg"><p>Your smile fixes everything.</p></section>
<section><img src="photo4.jpg"><p>You are my calm.</p></section>
<section><img src="photo5.jpg"><p>You are my chaos.</p></section>
<section><img src="photo6.jpg"><p>You are my home.</p></section>
<section><img src="photo7.jpg"><p>I choose you.</p></section>
<section><img src="photo8.jpg"><p>Always.</p></section>

<!-- FINAL LOVE -->
<section>
  <h1>I Love You, Vidhi ❤️</h1>
  <p>
    Since 11-11-2024,<br>
    my heart has known where it belongs.
  </p>
</section>

<!-- LETTER -->
<section class="letter">
  <h1>A Letter for You, Vidhi 💌</h1>

  <div class="candle" style="left:15%"></div>
  <div class="candle" style="left:35%"></div>
  <div class="candle" style="left:65%"></div>
  <div class="candle" style="left:85%"></div>

  <p style="font-family:Georgia,serif;font-size:1.15em;line-height:1.9;">
    Dear Vidhi,<br><br>

    Somewhere between our conversations,<br>
    your laughter, and your kindness,<br>
    you became my favorite part of every day.<br><br>

    On 11-11-2024, when you told me how you felt,<br>
    love stopped being a question<br>
    and became an answer.<br><br>

    I promise honesty, effort,<br>
    and a heart that chooses you —<br>
    every single day.<br><br>

    Always yours,<br>
    <strong>Takshak ❤️</strong>
  </p>
</section>

<script>
/* Reveal animation */
const sections = document.querySelectorAll("section");
const reveal = () => {
  sections.forEach(sec => {
    if (sec.getBoundingClientRect().top < window.innerHeight - 100) {
      sec.classList.add("show");
    }
  });
};
window.addEventListener("scroll", reveal);
reveal();

/* Typewriter */
const text = "I Love You, Vidhi ❤️";
let i = 0;
const target = document.getElementById("typeText");
function typeWriter() {
  if (i < text.length) {
    target.innerHTML += text.charAt(i++);
    setTimeout(typeWriter, 120);
  }
}
typeWriter();

/* Hearts */
setInterval(() => {
  const heart = document.createElement("div");
  heart.className = "heart";
  heart.innerHTML = Math.random() > 0.5 ? "❤️" : "💖";
  heart.style.left = Math.random() * 100 + "vw";
  heart.style.fontSize = (12 + Math.random() * 18) + "px";
  document.body.appendChild(heart);
  setTimeout(() => heart.remove(), 15000);
}, 900);

/* Music autoplay on interaction */
const music = document.getElementById("bgMusic");
const btn = document.getElementById("musicBtn");
let started = false;

function startMusic() {
  if (!started) {
    music.muted = false;
    music.volume = 0.4;
    music.play();
    started = true;
  }
}
document.addEventListener("click", startMusic);
document.addEventListener("scroll", startMusic);

btn.onclick = () => {
  if (music.paused) music.play();
  else music.pause();
};
</script>

</body>
</html>
